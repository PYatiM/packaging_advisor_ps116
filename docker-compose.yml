version: '3.8'
services:
  api:
    build:
      context: .
      dockerfile: deployment/Dockerfile.api
    ports:
      - "8000:8000"
    env_file:
      - .env
  ui:
    build:
      context: .
      dockerfile: deployment/Dockerfile.ui
    ports:
      - "8501:8501"
    depends_on:
      - api
